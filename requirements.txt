pandas==2.2.3
numpy==1.26.4
faker==30.8.2
jupyter==1.1.1
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.5.2
xgboost==2.1.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic-settings==2.6.1
# torch is a transitive dependency of sentence-transformers, but we pin an
# explicit CUDA-enabled build here (this machine has an NVIDIA RTX 3050)
# for GPU-accelerated embedding generation. Requires --extra-index-url
# below, since CUDA wheels aren't hosted on regular PyPI. On a machine
# without an NVIDIA GPU, replace this line with plain `torch==2.6.0` and
# drop the --extra-index-url line - sentence-transformers falls back to
# CPU automatically.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
sentence-transformers==3.2.1
faiss-cpu==1.9.0
pyarrow==17.0.0
# Pinned to versions compatible with the current httpx (older groq/anthropic
# releases call httpx with a removed 'proxies' kwarg and crash on import).
groq==1.7.0
anthropic==1.5.0
fastapi==0.115.5
uvicorn[standard]==0.32.1
locust==2.32.2